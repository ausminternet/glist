meta {
  name: POST uncheck
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v1/households/:householdId/shopping-lists/:listId/items/:itemId/uncheck
  body: none
  auth: inherit
}

params:path {
  householdId:
  listId:
  itemId:
}

headers {
  x-api-key: {{api_key}}
}
