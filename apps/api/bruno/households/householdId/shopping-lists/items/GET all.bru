meta {
  name: GET all
  type: http
  seq: 10
}

get {
  url: {{base_url}}/households/:householdId/shopping-lists/:shoppingListId/items
  body: none
  auth: inherit
}

params:path {
  householdId:
  shoppingListId:
}

headers {
  x-api-key: {{api_key}}
}

settings {
  encodeUrl: true
  timeout: 0
}
